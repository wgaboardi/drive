/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "sincere-panther-855.convex.cloud",
      }
    ]
  }
};

export default nextConfig;
